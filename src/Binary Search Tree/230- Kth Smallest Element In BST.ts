import { BinaryTree } from "./index";

// Pseudocode
// Perform inorder travesal (left => middle => right)
// track a counter
// during travesal , if the counter is k value , then return the value of the node

const kthSmallestElement = (bst: BinaryTree, k: number) => {};
